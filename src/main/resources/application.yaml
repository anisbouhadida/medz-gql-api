spring:
  application:
    name: medz-gql-api

  graphql:
    graphiql:
      enabled: true

  datasource:
    url: jdbc:postgresql://localhost:5432/medz
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
